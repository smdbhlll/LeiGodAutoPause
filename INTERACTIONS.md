# 雷神自动暂停 - 交互说明

## 状态归属

| 状态 | 所有者 | 范围 | 持久化 |
| --- | --- | --- | --- |
| `toggleStates` | `window` | 设置页开关的业务状态 | 保存设置时写入配置 |
| `toggleAnimation` | `window` | 单个活动开关的视觉进度 | 否，动画结束后立即删除 |
| `darkMode` | `window` | 全窗口主题 | 否，跟随 Windows 应用模式 |
| `currentView` | `window` | 当前导航页 | 否 |
| `busy` / `results` | `window` | 扫描、账户查询和暂停任务 | 否 |

## 开关事件流

1. 用户使用鼠标或键盘触发 Win32 `BUTTON` 的 `BN_CLICKED`。
2. 窗口先读取当前视觉进度，再立即更新 `toggleStates`。
3. `startToggleAnimation` 创建 200ms 的时间动画，并只为活动动画启动 8ms 计时器。
4. `WM_TIMER` 只重绘 `toggleAnimation` 中的控件，不刷新其余开关或整窗。
5. `drawToggleTrack` 在 4 倍离屏缓冲中绘制轨道和滑块，再以高质量模式缩小到控件。
6. 动画完成后删除视觉状态、停止计时器并恢复系统计时精度。
7. 用户快速反向点击时，从当前视觉位置开始新动画，避免跳变。

## 主题事件流

1. `WM_SETTINGCHANGE` 或每秒轻量轮询触发 `updateSystemTheme`。
2. 仅当 `AppsUseLightTheme` 的值变化时重建主题画刷。
3. 窗口、列表、输入框和开关在同一轮重绘中切换颜色。
4. `LEIGOD_THEME=light|dark` 可在视觉测试时覆盖系统主题。

## 异步状态

| 状态 | 表现 | 条件 |
| --- | --- | --- |
| 执行中 | 禁用重复操作，保留当前页面内容 | 扫描、查询或暂停任务进行中 |
| 空结果 | 列表为空并显示对应状态文本 | 扫描成功但没有候选项 |
| 错误 | 原生消息框或状态文本显示错误 | 后台任务失败 |
| 成功 | 更新列表、账户信息或暂停状态 | 后台任务完成 |

## 边界情况

- Windows 关闭客户端动画时，开关直接切换到目标状态。
- 动画仅持续约 200ms，计时器和 1ms 系统计时精度仅在活动期间启用。
- 离屏位图、画刷、区域和 DC 在每次绘制结束时释放，避免 GDI 句柄泄漏。
- 主题在动画期间变化时，下一帧直接使用新主题颜色。
- 页面隐藏或窗口进入托盘后，没有活动动画就不会产生额外重绘。
- 开关保留原生按钮语义和焦点行为，可通过键盘操作。
